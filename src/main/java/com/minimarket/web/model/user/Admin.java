package com.minimarket.web.model.user;

import jakarta.persistence.Entity;

@Entity
public class Admin extends User {
    // Atribut tambahan khusus untuk Admin bisa ditambahkan di sini jika dibutuhkan
}
